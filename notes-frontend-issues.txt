1. 1. Create modal context for globally managed instead of prop drilling > OKAY
2. USES:
    A. Home
        {isModalOpen,setIsModalOpen
        action,setAction
        post,setPost ??
        toggleModal //this is method
        }


    B. PostItem
        > to view post :
            toggleModal({type:''})
            setPost({post:post})

    C. PostCreate
        toggleModal({})



export to home js 
> isModalOpen (for setting modal for create post / view post)
> toggleModal (sets everything ex: action and opening of modal)
> action (for checking which to render)
> post (temp), setPost